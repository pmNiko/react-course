import { RootPaths } from "@/paths";

export const AuthPaths = {
  Login: {
    relative: "login",
    absolute: RootPaths.AUTH + "/login",
  },
  Register: {
    relative: "register",
    absolute: RootPaths.AUTH + "/register",
  },
};
